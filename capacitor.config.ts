import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.propswipes.propswipess',
  appName: 'propswipes',
  webDir: 'dist',
  ios: {
    scheme: 'PropSwipes'
  }
};

export default config;